In Xcode, select File > Add Package Dependencies…
Click Add Local… and choose <PROJECT_ROOT>/DebugTools/IOSDebugKit.
Add the IOSDebugKit product to the Debug configuration of the app target.
Add <PROJECT_ROOT>/DebugTools/IOSDebugBootstrap.swift to the app target and call IOSDebugBootstrap.start() from Debug startup code.
